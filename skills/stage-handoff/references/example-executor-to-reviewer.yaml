# Example: Executor → Reviewer Stage Handoff

contract_id: "EXAMPLE-001"
revision: 1
lifecycle_state: "review"

completed_actions:
  - action: "Fixed skip calculation: changed `page * size - 1` to `(page - 1) * size`"
    result: "One-line change applied"
    evidence_ref: "git diff a1b2c3d..b2c3d4e -- src/api/orders/pagination.ts"
    timestamp: "2026-07-24T14:10:00Z"
    actor: "coder (executor)"
  - action: "Added regression test: 3-page pagination (10, 10, 5 items)"
    result: "New describe block in pagination.test.ts"
    evidence_ref: "git diff a1b2c3d..b2c3d4e -- tests/api/orders/pagination.test.ts"
    timestamp: "2026-07-24T14:12:00Z"
    actor: "coder (executor)"
  - action: "Ran full test suite"
    result: "All 47 tests passed (2 new), exit code 0"
    evidence_ref: "npm test output (exit 0, 47 passing)"
    timestamp: "2026-07-24T14:15:00Z"
    actor: "coder (executor)"

outstanding_work:
  - description: "Review the diff for correctness, edge cases, and style"
    assigned_role: "reviewer"
    dependencies: []

blockers: []
dependencies: []

open_questions: []

risk_class: "low"
risk_changes: []

next_action:
  description: "Review the diff for correctness (is the formula right?), edge cases (page 0? page 1? last page?), and confirm the test actually covers the bug. Produce review findings as a stage output envelope."
  role: "reviewer"
  lane: "worker (fresh context)"
  context: "This is an independent review — you have NOT seen the executor's reasoning, only their output. The diff is at git diff a1b2c3d..b2c3d4e. Contract acceptance criteria: AC-1 (pagination counts), AC-2 (one-line change), AC-3 (all tests pass)."

artifacts:
  - ref: "b2c3d4e"
    type: "commit"
    description: "Head commit with the fix and test"
  - ref: "git diff a1b2c3d..b2c3d4e"
    type: "commit"
    description: "Full diff for review"
  - ref: "npm test output (exit 0, 47 passing)"
    type: "evidence_record"
    description: "Executor's test run evidence"

timestamp: "2026-07-24T14:16:00Z"
actor: "coder (executor)"
session_ref: "session-def456"  # reviewer does NOT inherit this session